version: '3.8'
services:
  postgres:
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-warroom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-warroom}
      POSTGRES_DB: ${POSTGRES_DB:-warroom}
  redis:
    command: ["redis-server", "--appendonly", "yes"]
