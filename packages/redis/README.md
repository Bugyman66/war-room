# Redis Package

This package provides utilities for publishing and consuming signals using Redis Streams.

- Use `publisher.ts` to publish normalized signals to the `signals` stream.
- Add consumer logic for multi-consumer processing and replay capability.
